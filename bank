#include <stdio.h>
#include <stdlib.h>
int main (void)
int main_exit;
{
    int i,j;
    char pass[10],pass[10]="mycap";
    printf("\n\n\tEnter the password to login:");
    scanf(%s,&pass);


if (pass[i]!=13 && pass[i]!=8)
{

    printf("");
    print[i]=getch();
    i++;
}
while (pass[i]!=13)
pass[10]="\0";
if (strcmp(pass,password==0))
{
    printf("\n\nPassword Match");
    for(int i=0; i<6;i++)
    {
        fordelay(1000);
        printf(" , ");
    }
    system("cls");
    menu();
}
else
{
    printf("\n\nWrong Password");
    printf("Enter 1 to retry and enter 0 to exit");
    scanf("%d",&main_exit);
    if (main_exit==1)
    {
        system("cls");
        menu();
    }
    else if (main_exit==1)
    {
        system("cls");
        close();
    }
    else
    {
        printf("\nInvalid");
        fordelay(1000);
        system("clr");
        goto login_try;
    }

}
return 0;
}

